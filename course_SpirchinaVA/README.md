## 📁 СТРУКТУРА ПРОЕКТА

### Основные файлы
- **`run_project.py`** ⭐ - Главная точка входа (единственный исполняемый файл)
- **`train_model.py`** - Модуль с функциями обучения моделей
- **`test_model.py`** - Модуль с функциями тестирования

### Модели и данные
- **`models/`** - Папка с обученной моделью
  - `final_model.pkl` - Обученная модель XGBoost
  - `scaler.pkl` - StandardScaler для нормализации данных
  - `feature_names.pkl` - Названия признаков (14 каналов ЭЭГ)
  - `model_info.pkl` - Метаинформация о модели (тип, метрики)
- **`data/`** - Папка с датасетом (создается автоматически)
  - `eeg_eye_state.arff` - Датасет EEG Eye State

### Результаты
- **`results/`** - Папка с результатами обучения
  - `class_distribution.png` - График распределения классов
  - `correlation_matrix.png` - Корреляционная матрица признаков
  - `model_comparison.png` - Сравнение всех обученных моделей
  - `test_confusion_matrix.png` - Матрица ошибок на тестовой выборке

---

## 🚀 ЗАПУСК ПРОЕКТА

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Для macOS с Python 3.13:

```bash
python3 -m pip install --break-system-packages -r requirements.txt
```

Для macOS может потребоваться установка OpenMP для XGBoost:

```bash
brew install libomp
```

### 2. Запуск проекта 

```bash
python3 run_project.py
```

**Что происходит:**
1. Проверка зависимостей
2. Загрузка данных (автоматическое скачивание если нужно)
3. Разделение датасета на train/test
4. Разведочный анализ данных (EDA)
5. Обучение 4 моделей: Logistic Regression, Random Forest, Gradient Boosting, XGBoost
6. Сохранение лучшей модели
7. Тестирование на тестовой выборке
8. Интерактивное меню для дополнительного тестирования

**Время выполнения:** ~3-5 минут




